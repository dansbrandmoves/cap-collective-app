# Reject npm packages published less than 7 days ago.
# Requires npm 10.0+. Verify with: npm config list
# This is a lightweight defense against supply chain attacks —
# most malicious package versions are caught and yanked within days.
min-release-age=7
